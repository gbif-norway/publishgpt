apiVersion: v1
kind: Service
metadata:
  name: {{ include "publishgpt.fullname" . }}
  labels:
    {{- include "publishgpt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "publishgpt.selectorLabels" . | nindent 4 }}
